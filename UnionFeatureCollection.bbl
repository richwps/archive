Layer one = parameters[0]
Layer two = parameters[1]

def proc = new Process("vec:UnionFeatureCollection")
Map results = proc.execute(["first feature collection": one, "second feature collection": two])

//wrap this procedure

Layer layer = new Layer()
layer.add(results.result)

features = GeoscriptHelper.prepare(layer)

outputs = ["one": features]